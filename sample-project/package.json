{
  "name": "sample-project",
  "version": "0.0.11",
  "description": "ample-project",
  "types": "dist/src/index.d.ts",
  "main": "dist/src/index.js",
  "typings": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc ",
    "build-watch": "tsc -w",
    "clean": "rm -rf dist docs api",
    "test": "jest --config ./jest.config-spec.js && npm run test-standalone",
    "test-watch": "npm test -- --watch",
    "test-standalone": "npx ts-node -P ./tsconfig.json --files src/__tests__/testWithoutJest.ts -P",
    "prettier": " npx prettier  --write --arrow-parens avoid --parser typescript --use-tabs false --no-semi true --single-quote true --tab-width 2 --trailing-comma all --end-of-line lf --prose-wrap preserve --print-width 120 --no-bracket-spacing  --jsx-bracket-same-line true \"src/**/*.ts*\"",
    "all": "npm run clean && npm run prettier && npm run build && npm run test && npm run docs",
    "docs": "npx typedoc src/types.ts src/checkType.ts src/expectType.ts --theme markdown --out api",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^10.12.18",
    "jest": "^24.5.0",
    "ts-jest": "^24.0.0",
    "tsd-check-runtime": "file:..",
    "typescript": "^3.3.3333"
  },
  "dependencies": {},
  "keywords": [
    "JavaScript",
    "TypeScript",
    "utilities",
    "miscellaneous"
  ],
  "author": "Sebastián Gurin",
  "repository": {
    "type": "git",
    "url": "https://github.com/cancerberoSgx/misc-utils-of-mine.git"
  },
  "license": "MIT"
}
